import Component, { ComponentProps } from '@/base/component';

type ${componentClassName}Options = {

}

export default class ${componentClassName} extends Component<HTMLElement, ${componentClassName}Options> {
    constructor(element: ComponentProps<HTMLElement>) {
        super(element);
    }

    destroy() {
        // Destroy function
    }
}
