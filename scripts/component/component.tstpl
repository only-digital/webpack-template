import Component, { ComponentProps, ComponentOptions } from '@/base/component';

interface ${componentClassName}Options extends ComponentOptions {

}

export default class ${componentClassName} extends Component {
    constructor(element: ComponentProps, options?: ${componentClassName}Options) {
        super(element);
    }

    destroy = () => {
        // Destroy function
    }
}
